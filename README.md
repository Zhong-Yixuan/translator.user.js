# translator.user.js

选中文本自动翻译

// notice:

// 代码由AI生成后人工调试，由于 API 接口更换，fetchexplaination 的代码有些抽象，没有用优雅函数保证每个人都能看，用工具格式化一下就可以了。

// 由于经过手动调试，内留下大量人工注释，方便理解（因为我也是初学）。

// 由于国内没有免费的翻译 API，国内可以调用的 dictionaryapi.dev 不太稳定，freedictionaryapi.com 传回的无用信息过多，所以选择词典 API 是 Wiktionary API, 翻译是 Google translate, 都需要连接 VPN，使用十分稳定。

// 为了确保信息密度，对于一些简单且难以解释的词如 from 等不会展示翻译框，这不是 bug。可以手动在第 17 行定义的数组中删除 "preposition" 这一项，即可展示。注意上面元素后面的逗号也一并删去。包括它会略过翻译仅中文，但仍翻译韩语等

// style 框的样式在 360 行开始到 </style> 结束。可以用 AI 优化，但是不要删除任何一个 .Zyx，要清除整体样式把 .Zyx{}  内的全部删除即可。

// 默认仅显示一个句子示例，如有需求可以在 28 行更改 explaination_Show 的值。

// 点击任意位置后框消失，但是在悬浮框最上方的位置，鼠标样式改变时可以拖动位置

// 后续还会进行优化，有问题反馈或希望加入的功能直接私信即可，目前方向：点击样式上按钮显示更多例句，尝试使用多个 API 以得到中文解释（这个有些麻烦，目前没有好的方案），滑条等
